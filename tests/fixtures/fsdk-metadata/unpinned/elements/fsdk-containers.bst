# Fixture: elements/fsdk-containers.bst, trimmed to what the checker reads.
kind: junction
sources:
  - kind: git_repo
    url: github:projectbluefin/fsdk-containers.git
    track: main
config:
  options:
    arch: '%{arch}'
